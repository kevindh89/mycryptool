<form method="POST" action="{{ url('account_update') }}">
    <div class="form-group form-dark p-3">
        <div class="row">
            <div class="col-sm-4 text-right">
                <label for="start-capital" class="m-2">Start capital:</label>
            </div>
            <div class="col-sm-4">
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text bg-dark text-light">€</span>
                    </div>
                    <input type="text" class="form-control text-light bg-dark" id="start-capital" name="start-capital" value="{{ app.request.session.get('startCapital', 0) }}" />
                </div>
            </div>
            <div class="col-sm-4">
                <button type="submit" class="btn btn-secondary">Save</button>
            </div>
        </div>
    </div>
</form>